import { describe, expect, it } from 'vitest';

// Consolidated admin hierarchy view deprecated; keep placeholder so Vitest ignores legacy suite.
describe.skip('AdminHierarchyPage legacy placeholder', () => {
  it('confirms the summary dashboard route has been removed', () => {
    expect(true).toBe(true);
  });
});
