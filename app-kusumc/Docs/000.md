If not present in the list, can you also plan to add internal testing mechanims so you can operate the app via ADB script, execute the Auth and Simulated run for end to end Simulator which actually hits all the necesarry APIs and MQTT work end to end Pub and Sub cyle through the MQTT broker and also check the data being persisted across all API actions (User side roles like create device and add and allocate beneficiary and installation details) then also after running the device side roles of Bootstrap and use those local creds and connect to broker and publish data on all the topics and also subscribe to them, once few packets are sent check the subscribed data loop and then also check the data persisted via APIs as user roles then rotate Device creds and then check if the device side role of mqtt connection is dropped by the server, once device gets connection rejection, the device roles has to fetch new creds by bootstrap and then repaeat the MQTT connection, Pub , Sub check and DB persistence check as a long-automated-mobile-auth-to-persistence as a long test with sequential plan and evaluate Sequnce diagram of User Role - Backend - Broker - Device Role - User Role plan. 