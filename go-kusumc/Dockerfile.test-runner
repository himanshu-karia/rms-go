FROM golang:1.24

RUN apt-get update \
    && apt-get install -y --no-install-recommends postgresql-client netcat-openbsd ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

COPY go.mod go.sum ./
RUN go mod download
